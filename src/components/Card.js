import styled from 'styled-components';

const Card = styled.div`
  width: 400px;
  background: #fff;
  margin: 10px 20px;
  padding: 20px;
  border-radius: 4px;
  box-shadow: 0 1px 3px rdba(0, 0, 0, 0, 12), 0 1px 2 px rgba(0, 0, 0, 0, 24);
`;

export default Card;